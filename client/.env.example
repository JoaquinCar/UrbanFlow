# Solo hacen falta en desarrollo, donde el cliente (5173) y la API (3000) están
# en puertos distintos. En producción se dejan sin definir: el cliente usa el
# mismo origen que sirvió la página, que es como los entrega nginx.
VITE_API_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
